#include<stdio.h>

int main(){

char ch;
    scanf("%c",&ch);
    if(ch >= 65 && ch <= 90) 
    {
        printf("%c is a lower case alphabet",ch);
    }
    else{
        printf("%c is a lower case alphabet",ch);
    }
}
